apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wp01-ingress
spec:
  rules:
  - host: first.rockandcode.ir
    http:
      paths:
      - pathType: Prefix
        path: /
        backend:
          service:
            name: wordpress
            port:
              number: 80